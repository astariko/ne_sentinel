<%= form_for(@ne) do |f| %>
  <% if @ne.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ne.errors.count, "error") %> prohibited this NE from being saved:</h2>

      <ul>
      <% @ne.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :ip %><br>
    <%= f.text_field :ip %>
  </div>
  <div class="field">
    <%= f.label :ectype %><br>
    <%= f.text_field :ectype %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
